{
  "name": "@sohopay/agent-skills",
  "private": true,
  "version": "0.1.0",
  "description": "SohoPay agent skills — hosted docs and open-registry packages",
  "scripts": {
    "validate": "node scripts/validate-skills.mjs",
    "generate:hosted": "node scripts/generate-hosted.mjs",
    "generate:llms-full": "node scripts/generate-llms-full.mjs",
    "build": "npm run generate:hosted && npm run generate:llms-full"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0"
}
